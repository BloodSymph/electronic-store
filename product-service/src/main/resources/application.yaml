spring:
  application:
    name: product-service
  config:
    import: optional:configserver:http://localhost:8888/
  web:
    resources:
      static-locations: classpath:D:\PetProjects\static\img\**
